default:
    suites:
        core_features:
            paths:    [ %paths.base%/features/core ]
            contexts: [ FeatureContext ]
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~